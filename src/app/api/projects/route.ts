import { NextResponse } from 'next/server'
import { requireClerkUserId } from '@/lib/auth/require-user'
import { projectsRepository } from '@/lib/appwrite/repositories/projects.repository'
export async function GET() { try { return NextResponse.json(await projectsRepository.list(await requireClerkUserId())) } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : 'Unexpected server error.' }, { status: 400 }) } }
export async function POST(request: Request) { try { const body = await request.json() as { name?: unknown; description?: unknown; color?: unknown; icon?: unknown }; const name = typeof body.name === 'string' ? body.name.trim() : ''; if (!name || name.length > 120) throw new Error('Project name must be between 1 and 120 characters.'); return NextResponse.json(await projectsRepository.create(await requireClerkUserId(), { name, description: typeof body.description === 'string' ? body.description : '', color: typeof body.color === 'string' ? body.color : '', icon: typeof body.icon === 'string' ? body.icon : '', archived: false }), { status: 201 }) } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : 'Unexpected server error.' }, { status: 400 }) } }
